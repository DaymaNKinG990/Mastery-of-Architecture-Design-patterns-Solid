# 🎨 Возможности редактора кода

Интерактивные упражнения в этом курсе оснащены продвинутым редактором кода на базе **CodeMirror**, который предоставляет удобные функции для написания и редактирования Python кода.

## ✨ Основные возможности

### 1. 🎨 Подсветка синтаксиса
- Автоматическая подсветка синтаксиса Python
- Тема Monokai (темная) или светлая тема в зависимости от настроек
- Визуальное выделение ключевых слов, строк, комментариев

### 2. ⌨️ Умная работа с клавишами

#### Tab для отступов
- **Tab** - добавляет 4 пробела (стандартный отступ Python)
- **Shift + Tab** - убирает отступ
- **Tab** при выделенном блоке - добавляет отступ всему блоку
- **Shift + Tab** при выделенном блоке - убирает отступ у всего блока

#### Быстрый запуск кода
- **Ctrl + Enter** (Windows/Linux) или **Cmd + Enter** (macOS) - запустить код
- Не нужно тянуться мышкой к кнопке!

### 3. 🔍 Подсказки при редактировании

#### Автозакрытие скобок
- При вводе `(` автоматически добавляется `)`
- При вводе `[` автоматически добавляется `]`
- При вводе `{` автоматически добавляется `}`
- При вводе `"` автоматически добавляется `"`
- При вводе `'` автоматически добавляется `'`

#### Подсветка парных скобок
- Когда курсор находится рядом со скобкой, автоматически подсвечивается парная скобка
- Помогает отследить правильность вложенности

### 4. 📝 Удобное редактирование

#### Номера строк
- Каждая строка пронумерована
- Легко ориентироваться в коде и находить ошибки

#### Подсветка активной строки
- Строка, на которой находится курсор, выделяется фоном
- Помогает не потеряться в коде

#### Перенос длинных строк
- Длинные строки автоматически переносятся
- Не нужно прокручивать горизонтально

### 5. 📋 Копирование и вставка

Редактор полностью поддерживает стандартные операции:

- **Ctrl + C** / **Cmd + C** - копировать
- **Ctrl + V** / **Cmd + V** - вставить
- **Ctrl + X** / **Cmd + X** - вырезать
- **Ctrl + A** / **Cmd + A** - выделить всё
- **Ctrl + Z** / **Cmd + Z** - отменить
- **Ctrl + Shift + Z** / **Cmd + Shift + Z** - повторить

### 6. 🎯 Выделение кода

- **Двойной клик** - выделить слово
- **Тройной клик** - выделить строку
- **Shift + стрелки** - выделение по символам/строкам
- **Ctrl + Shift + стрелки** - выделение по словам

## 🔧 Технические детали

### Используемые технологии

- **CodeMirror 5.65.16** - современный редактор кода для веб
- **Python mode** - специальный режим для Python с полной поддержкой синтаксиса
- **Monokai theme** - популярная цветовая схема для кода
- **Pyodide** - Python в браузере для выполнения кода

### Fallback режим

Если по какой-то причине CodeMirror не загрузится (проблемы с CDN, блокировщик рекламы и т.д.), автоматически включается fallback режим:

- Используется обычный `<textarea>`
- Но с поддержкой **Tab** для отступов
- **Ctrl/Cmd + Enter** для запуска кода
- Все базовые функции работают

## 💡 Советы по использованию

### Для новичков

1. **Не бойтесь экспериментировать** - можно всегда нажать кнопку "🔄 Сбросить"
2. **Используйте Tab** - правильные отступы важны в Python
3. **Следите за подсветкой скобок** - помогает избежать синтаксических ошибок

### Для опытных разработчиков

1. **Ctrl/Cmd + Enter** - самый быстрый способ запустить код
2. **Shift + Tab** для исправления отступов блока кода
3. Можно копировать код из вашего любимого редактора и вставлять сюда

## 🐛 Возможные проблемы

### Редактор не загружается

**Причины:**
- Блокировщик рекламы блокирует CDN
- Проблемы с интернет-соединением
- Устаревший браузер

**Решение:**
- Отключите блокировщик для этого сайта
- Проверьте соединение
- Используйте современный браузер (Chrome, Firefox, Safari, Edge)

### Tab не работает

**Причина:** CodeMirror не инициализировался

**Решение:**
- Обновите страницу
- Если не помогло, fallback режим должен автоматически обработать Tab

### Тормозит на больших файлах

**Причина:** Слишком много кода (>1000 строк)

**Решение:**
- Разделите код на меньшие функции
- Упражнения не предполагают такие большие объемы

## 📚 Дополнительные ресурсы

- [CodeMirror Documentation](https://codemirror.net/5/doc/manual.html)
- [Python Style Guide (PEP 8)](https://peps.python.org/pep-0008/)
- [Pyodide Documentation](https://pyodide.org/)

---

**Приятного обучения! 🚀**

