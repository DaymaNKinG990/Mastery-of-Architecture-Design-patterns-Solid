# 🚀 Быстрый старт: Тестирование нового редактора кода

## Что было сделано?

Интегрирован **CodeMirror** - профессиональный редактор кода с:
- ✅ **Tab работает правильно** (добавляет отступы вместо переключения фокуса)
- ✅ Подсветка синтаксиса Python
- ✅ Автозакрытие скобок
- ✅ Ctrl/Cmd+Enter для запуска кода
- ✅ Копирование/вставка работает корректно

## 🧪 Быстрый тест (2 минуты)

### Вариант 1: Автономный тест

```bash
# Просто откройте в браузере
docs/test-code-editor.html
```

Проверьте:
- [ ] Подсветка синтаксиса Python
- [ ] Tab добавляет 4 пробела
- [ ] Shift+Tab убирает отступ
- [ ] Ctrl/Cmd+Enter запускает код
- [ ] Копирование/вставка работает

### Вариант 2: Полный тест через MkDocs

```bash
# Запустите локальный сервер
mkdocs serve

# Откройте браузер
# http://127.0.0.1:8000/
```

Перейдите к упражнению:
- Single Responsibility Principle → Упражнение 1
- Попробуйте все функции редактора

## ✨ Новые возможности

### Горячие клавиши

| Клавиши | Действие |
|---------|----------|
| **Tab** | Добавить отступ (4 пробела) |
| **Shift+Tab** | Убрать отступ |
| **Ctrl/Cmd+Enter** | Запустить код |
| **Ctrl+Z** | Отменить |
| **Ctrl+Shift+Z** | Повторить |

### Автоматические функции

- **Автозакрытие:** Введите `(` → автоматически добавится `)`
- **Подсветка скобок:** Курсор у скобки → подсвечивается парная
- **Номера строк:** Автоматически для всех упражнений

## 📁 Структура изменений

```
Новые файлы:
├── docs/assets/js/code-editor.js          # Логика редактора
├── docs/assets/css/code-editor.css        # Стили редактора
├── docs/CODE_EDITOR_FEATURES.md           # Документация
├── docs/CODE_EDITOR_UPGRADE.md            # Технические детали
└── docs/test-code-editor.html             # Тестовая страница

Изменённые файлы:
├── mkdocs.yml                             # CDN ссылки
├── main.py                                # code_input_form
└── docs/assets/js/pyodide-exercise.js     # Интеграция
```

## 🔧 Технические детали

### Используется:
- **CodeMirror 5.65.16** (стабильная версия)
- **Python mode** для подсветки синтаксиса
- **Monokai theme** (тёмная тема)
- **CDN:** cloudflare (быстрая загрузка)

### Fallback:
Если CodeMirror не загрузится:
- Автоматически используется обычный textarea
- Tab обрабатывается через JavaScript
- Все функции сохраняются

## ❓ FAQ

**Q: Нужно ли что-то устанавливать?**  
A: Нет! Всё загружается с CDN автоматически.

**Q: Повлияет ли это на существующие упражнения?**  
A: Нет, всё работает автоматически без изменений.

**Q: Что если у меня медленный интернет?**  
A: CodeMirror кэшируется браузером. После первой загрузки работает мгновенно.

**Q: Поддерживается ли на мобильных?**  
A: Да, но лучше работает на десктопе (больше места для кода).

## 📚 Дополнительная документация

- **Для пользователей:** `docs/CODE_EDITOR_FEATURES.md`
- **Технические детали:** `docs/CODE_EDITOR_UPGRADE.md`
- **MkDocs конфигурация:** `mkdocs.yml` (extra_javascript секция)

## 🎯 Проверка работоспособности

Откройте консоль браузера (F12) и введите:

```javascript
// Должно вернуть true
typeof CodeMirror !== 'undefined'
typeof window.initCodeEditor === 'function'
```

Если оба возвращают `true` - всё работает! ✅

## 🚀 Деплой

Для публикации на GitHub Pages:

```bash
# Соберите сайт
mkdocs build

# Опубликуйте
mkdocs gh-deploy
```

Или через GitHub Actions (автоматически при push).

---

**Готово к использованию!** 🎉

Если есть вопросы, смотрите полную документацию в `docs/CODE_EDITOR_UPGRADE.md`

